# Backend Architecture

- Node.js and Express with JavaScript are used for the backend.
- Keep API responsibilities separate from frontend rendering.
- Supabase is the project service for authentication and PostgreSQL infrastructure.
- Add routes and persistence only for explicitly requested behavior.
