# Backend Rules

- Use JavaScript only; do not introduce TypeScript.
- Keep the API minimal and avoid premature abstractions.
- Never expose service-role keys, database passwords, OAuth secrets, or other credentials.
- Do not create database tables, mock production data, or unrelated routes without an explicit task.
- Read existing backend code before editing and preserve unrelated behavior.
